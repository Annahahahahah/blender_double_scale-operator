# blender_double_scale-operator 
A simple Python operator that doubles the value. 
Простой оператор на Python, который увеличивает в 2 раза.
# how it works + how to use this
- Install the add‑on via Preferences -> Add‑on -> Install.
- Select an object and launch the operator from the panel.
# screenshot
